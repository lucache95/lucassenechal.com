---
plan: "08-04"
phase: "08-sms-channel"
title: "Fix outbound reply direction in sms_send_log"
wave: 1
depends_on: []
autonomous: true
gap_closure: true
requirements: [SMS-06]
files_modified:
  - app/api/webhooks/twilio/route.ts
must_haves:
  truths:
    - "Outbound TwiML replies are logged with direction: 'outbound' in sms_send_log"
---

# Plan 08-04: Fix outbound reply direction in sms_send_log

<objective>
Fix the direction field on the TwiML reply log entry in the Twilio webhook handler. Currently logs outbound replies as 'inbound' — should be 'outbound' since TwiML responses are messages sent TO the subscriber.
</objective>

## Tasks

<task id="08-04-01" type="auto">
<title>Change direction from 'inbound' to 'outbound' for TwiML reply log</title>

<read_first>
- app/api/webhooks/twilio/route.ts (line ~250, the sms_send_log insert for assistant reply)
</read_first>

<action>
In `app/api/webhooks/twilio/route.ts`, find the sms_send_log insert at approximately line 247-252 (step 13 comment "Log outbound reply in sms_send_log").

Change:
```typescript
direction: 'inbound',
```
To:
```typescript
direction: 'outbound',
```

This correctly models that TwiML responses are outbound messages from the system to the subscriber.
</action>

<verify>
<automated>grep -n "direction: 'outbound'" app/api/webhooks/twilio/route.ts | grep -c "outbound"</automated>
</verify>

<acceptance_criteria>
- app/api/webhooks/twilio/route.ts contains `direction: 'outbound'` in the assistant reply sms_send_log insert (around line 250)
- No remaining `direction: 'inbound'` for outbound reply logging in the webhook handler
</acceptance_criteria>
</task>
